/* Fixture configuration for json_c_version.c only.
 * json-c generates config.h from cmake/config.h.in. This translation unit
 * uses none of its feature probes. No MSVC DLL exports or C++ mode are enabled.
 * This is a local configuration, not an upstream source file.
 */
